package com.mealuet.create_originium_industry.config;

/**
 * How much Originium Industry UI (goggles, sickness HUD, debug overlay) to show.
 */
public enum UiDetailLevel {
    MINIMAL,
    STANDARD,
    VERBOSE
}
